apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "min-auth.fullname" . }}-test-connection"
  labels:
    {{- include "min-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "min-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
