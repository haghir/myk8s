replicaCount: 1

image:
  repository: <repository>
  pullPolicy: IfNotPresent
  tag: latest

service:
  type: ClusterIP
  port: 80

db:
  host: <MySQL host>
  port: 3306
  user:
    secretName: <secret name>
    keys:
      username: username
      password: password
  dbname: <dbname>

storages:
  files:
    size: 10Gi
    path: <path to the directory>
    hostname: <hostname>
